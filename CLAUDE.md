@AGENTS.md

# Claude Code adapter

Read `.agents/agent-interop.md` for the shared boundary between persistent instructions, on-demand skills, and optional hooks. Claude-specific project skills are thin adapters under `.claude/skills/`; the canonical packaged skills remain under `skills/`.
